@using Modelo.Entidades
@model Clasificacion
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>ELIMINAR clasificacion</h1>
<p>Formulario para borrar una clasificacion en el sistema SCP</p>

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h3>Seguro que desea eliminar la clasificacion: @Model.MarcaId</h3>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-10">
            <form method="post" asp-action="Delete">
                <input type="hidden" asp-for="MarcaId" />

                <div class="form-group">
                    <label class="control-label">Marca</label>
                    <input class="form-control" asp-for="MarcaId" disabled/>
                </div>
                <div class="form-group">
                    <label class="control-label">Producto</label>
                    <input class="form-control" asp-for="ProductoId" disabled/>
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Eliminar la clasificacion" />
                </div>
            </form>
        </div>

    </div>
</div>

