@using Modelo.Entidades;
@model IEnumerable<Clasificacion>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>CLASIFICACIONES</h1>
<p>Se presenta a continuación la lista de las clasificaciones registradas en el sistema SCP</p>

<div class="container">
    @if (TempData["mensaje"] != null)
    {
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            @TempData["mensaje"]
            <button type="button" class="close" data-dismiss="alert" aria-label="close">
                <span aria-hidden="true">
                    &times;
                </span>
            </button>
        </div>
    }
    <div class="row">
        <div class="col-sm-6">
            <h3>Lista de clasificaciones</h3>
        </div>
        <div class="col-sm-6">
            <a class="btn btn-success d-block"
               asp-controller="clasificacion" asp-action="Create">
                Crear nueva clasificaciones
            </a>
        </div>
    </div>

    <div class="row">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <td>Marca</td>
                    <td>Producto</td>
                    <td>Acciones</td>
                </tr>
            </thead>
            <tbody>
                @foreach (Clasificacion clasificacion in Model)
                {
                    <tr>
                        <td>@clasificacion.Marca.MarcaId</td>
                        <td>@clasificacion.Producto.ProductoId</td>
                        <td>
                            <a class="btn btn-warning"
                               asp-controller="clasificacion" asp-action="Edit" asp-route-id="@clasificacion.MarcaId">
                                Editar
                            </a>
                            <a class="btn btn-danger"
                               asp-controller="clasificacion" asp-action="Delete" asp-route-id="@clasificacion.MarcaId">
                                Borrar
                            </a>
                        </td>
                    </tr>
                }

            </tbody>
        </table>

    </div>
</div>



