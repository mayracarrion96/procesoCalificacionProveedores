@using Modelo.Entidades
@model Oferta
@{
    ViewData["Title"] = "Delete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<h1>ELIMINAR OFERTA</h1>
<p>Formulario para borrar una oferta en el sistema SCP</p>

<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <h3>Seguro que desea eliminar la oferta: @Model.OfertaId</h3>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-10">
            <form method="post" asp-action="Delete">
                <input type="hidden" asp-for="OfertaId" />

                <div class="form-group">
                    <label class="control-label">Score Buro</label>
                    <input class="form-control" asp-for="ScoreBuro" disabled />
                </div>
                <div class="form-group">
                    <label class="control-label">Periodo</label>
                    <input class="form-control" asp-for="PeriodoId" disabled />
                </div>
                <div class="form-group">
                    <input type="submit" class="btn btn-primary" value="Eliminar la oferta" />
                </div>
            </form>
        </div>

    </div>
</div>